# Dépendances
node_modules/

# Secrets / config d'environnement
# ⚠️ Ne JAMAIS versionner : CLERK_SECRET_KEY, DATABASE_URL, GEMINI_API_KEY.
# Ces valeurs vivent uniquement dans les variables d'environnement Vercel.
# Seule la clé Clerk « publishable » (pk_live_…) est publique : elle est en dur
# dans public/js/clerk.js, c'est normal.
.env
.env.local
.env*.local

# Logs & divers
npm-debug.log*
*.log
.DS_Store

# Build / cache
dist/
.cache/
.vercel/
